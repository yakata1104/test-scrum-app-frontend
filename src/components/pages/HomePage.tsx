import { Box, Heading, Text } from "@chakra-ui/react";

export const HomePage = () => {
  return (
    <Box p={8}>
      <Heading>ホーム</Heading>
      <Text mt={4}>プロジェクト一覧を表示します。</Text>
    </Box>
  );
};
